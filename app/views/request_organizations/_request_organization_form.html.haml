= form.input :request_id, :as => :hidden
= form.input :organization, :label => "Name", :as => :autocomplete, :autocomplete_url => organizations_path(:format => :json), :related_attribute_name => :name

= form.commit_button
