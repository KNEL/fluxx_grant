- @models.compact.each do |model|
  %li.entry{'data-model-id' => model.report_id}
    %a.to-detail{:href => url_for(:action => :show, :id => model.report_id, :fluxxreport_filter => (model.filter_template ? 1 : nil))}
      %h2
        = model.report_label